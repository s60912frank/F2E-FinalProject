<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="./css/common.css">
  <link rel="stylesheet" href="./css/search.css">
  <title><%= search.text %>的搜尋結果</title>
</head>
<body>
  <div id="HamburgerMenu">
    <a href="/"><img id="hamLogo" src="./image/logo.png" /></a>
    <ul>
      <li>
        <% if(search.isAuthenticated){ %>
          <a href="/mypage"><%= search.user.name %></a>
        <% } else { %>
          <a href="/auth/facebook">Facebook登入</a>
        <% } %>
      </li>
      <li id="hamNick">
        <% if(search.isAuthenticated){ %>
          <p>目前暱稱:<%= search.user.nickname %></p>
        <% } %>
      </li>
      <li><a href="/issueList">議題列表</a></li>
      <li>
        <% if(search.isAuthenticated){ %>
          <a href="/post">發表議題</a>
        <% } %>
      </li>
      <li>
        <% if(search.isAuthenticated){ %>
          <a href="/logout">登出</a>
        <% } %>
      </li>
      <li id="hamSearch">
        <input type="text" id="searchInput">
        <img src="./image/searchIcon.png" id="searchIcon">
      </li>
      <li><a href="/aboutus">關於我們</a></li>
    </ol>
  <img src="./image/blackCross.png" alt="" id="hamCross">
  </div><!--place between <body> and <header>-->
  <div id="shadeCover">
  </div>
  <header><!---->
    <div class="container">
      <a href="/"><img id="Logo" src="./image/logo.png" /></a>
      <img id="HamburgerIcon" src="./image/hamburgerIcon.png" />
    </div>
  </header>

  <div class="container">
    <h1><%= search.text %> 的搜尋結果</h1>
    <a href="./issueList.html"><<返回列表</a>
    <div id="searchResultContainer">
    <% if(search.result){
         if(search.result.length > 0){
           for(var i = 0; i < search.result.length; i++){
             var link = "/issue?title=" + search.result[i].name;
             var date = search.result[i].time;
             var timeString = (date.getMonth() + 1) + '/' + date.getDate() + ' ' + date.getHours() + ":" + date.getMinutes();%>
             <div class="searchResult">
               <a href=<%= link %>>
                 <div class="titleContainer"><p><%= search.result[i].name %></p></div>
               </a>
               <div class="dateContainer"><p><%= timeString %></p></div>
               <div class="messageCount">
                 <img src="./image/messageIcon.png" alt="">
                 <p><%= search.result[i].commentsCount %></p>
               </div>
             </div>
           <% } %>
        <% } else{ %>
           <div class="noResult"><h1>查無結果!</h1></div>
        <% } %>
    <% } %>
    </div>
  </div>

  <footer class="fillParentWidth">
    <div class="container" id="foooooter">
      <p>Designed by TanKevin Design Studio</p>
      <p>Not responsible for any comments in this website</p>
      <p>NTUTEECS 2016</p>
    </div>
  </footer>
  <script src="./js/jquery-3.0.0.min.js"></script>
  <script src="./js/hamburgerMenu.js"></script>
</body>
</html>
