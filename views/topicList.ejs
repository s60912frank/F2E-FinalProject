<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>議題列表</title>
</head>
<body>
  <h1>議題列表</h1>

  <%if (isAuthenticated) { %>
    <h1>嗨! <span id="userName"><%= user.name %></span>! 準備好嘴砲了嗎?</h1>
    <p>你現在的身分是:<%= user.nickname %></p>
    <%if (user.isAdmin) { %>
      <!--只有是管理者才會出現這個按鈕-->
      <a href="/admin"><button>後臺管理</button></a>
    <% } %>
    <a href="/hot">熱門議題</a><br>
    <input type="text" id="nicknameText">
    <button id="changeNickname">更改暱稱</button>
    <button id="logout">登出</button>
    <div>
      增加主題: <input type="text" id="topicText">
      <button id="addTopic">送出</button>
    </div>
    <p>你已經登入!!</p>
  <% }else{ %>
    <p>你沒有登入!!</p>
    <a href="/auth/facebook">
      <button>以FB登入</button>
    </a><br>
    <input type="text" id="searchText">
    <button id="search">議題搜尋</button><br>
  <% } %>
  <input type="text" id="searchText">
  <button id="search">議題搜尋</button><br>

  <ul>
    <% for(var i = topics.length - 1; i >= 0; i--) {
      var link = "/topic?title=" + topics[i].name; %>
      <li><a href=<%= link %>><%= topics[i].name %></a></li>
    <% } %>
  </ul>
  <script src="./js/jquery-2.2.4.min.js"></script>
  <script src="./js/topicList.js"></script>
</body>
</html>
